export
	Debe enlistarlo en orden
	Debe devolver el valor de la clave entre comillas dobles
	No debe aparecer ninguna key empezada por _
	Se ha de proteger shlvl.                                                           Comprobados y correctos.
	Las diferencias en pwd, oldpwd etc depende del entorno, ha de comprobarse en 42

despues de cada prueba: export y echo $?

export NEW=foo                                                   aparece la variable foo STATUS = 0
export EMPTY=                                                   aparece la variable vacia STATUS = 0
export MSG="hello world"                                          aparece la variable STATUS = 0
export REPLACE=old
export REPLACE=new                                               Aparece la variable y ha sido remplazada STATUS = 0
export A=1 B=2 C=3                                          aparecen las variables y se crean a la vez STATUS = 0
export 1BAD=foo                                         minishell: export: `1BAD=foo': not a valid identifier STATUS = 1
export GOOD=ok BAD-NAME=1 ALSO_OK=2 1BAD=3        minishell: export: `BAD-NAME=1': not a valid identifier minishell: export: `1BAD=3': not a valid identifier STATUS = 1
export _OK=1                                                 es valido STATUS = 0
export __=x                                                  es valido STATUS = 0

unset ONLYNAME
export ONLYNAME
export                                                     # debe salir una línea:  declare -x ONLYNAME
env                                                        no debe contener ONLYNAME
echo "[$ONLYNAME]"

unset X Y
export X=1 2BAD=Z Y=3
echo $?                     # 1
echo "$X" "$Y"              # 1 3                          Debe salir en error en 2BAD=Z, pero declarar bien las otras dos.

unset L
L=123
export L
export | grep '^declare -x L='
env    | grep '^L='
echo $?                                                  Coorecto STATUS = 1

unset Z
export Z=          # valor vacío
export | grep '^declare -x Z='   # declare -x Z=""
env    | grep '^Z='              # Z=
unset Z
export Z                           # solo nombre
export | grep '^declare -x Z$'     # sin '='
env | grep '^Z='                                          Coorecto, no sale en env STATUS = 1

export S=' a  b '
printf '[%s]\n' "$S"     # [ a  b ]
export | grep '^declare -x S='
echo $?                                                  Coorecto, no sale en env STATUS = 1
